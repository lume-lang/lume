Package "sample" {
    version = "1.0.0"
    lume_version = "^0"
    description = "A sample Lume project using Arc"
    license = "MIT"
    repository = "https://github.com/lume-lang/lume"

    dependencies = [
        Dependency {
            source = "https://github.com/lume-lang/lume"
            version = "=1.0.0"
        }
    ]
}
