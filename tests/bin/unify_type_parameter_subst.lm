//# test-return = 0

fn main() {
    let array = [1, 2, 3, 4, 5];
    let iter = array.as_iter();

    loop {
        switch std::iter::Iterator::next(iter) {
            std::Optional<Int32>::Some(value) => { },
            std::Optional<Int32>::None => {
                break;
            },
        }
    }
}
